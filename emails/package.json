{
  "name": "truffl-emails",
  "private": true,
  "version": "1.0.0",
  "description": "Branded React Email templates for Truffl Pets (transactional notifications + auth)",
  "scripts": {
    "dev": "email dev",
    "export": "email export --outDir out",
    "build": "email export --outDir out",
    "send": "email export --outDir out && node send.mjs"
  },
  "dependencies": {
    "@react-email/components": "^1.0.12",
    "@react-email/render": "^2.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "react-email": "^6.8.1",
    "typescript": "^7.0.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
